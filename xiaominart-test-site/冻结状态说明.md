# xiaominART 待验收测试版冻结状态

> 冻结日期：2026-09-13 12:42（Asia/Shanghai）
> 项目：xiaominART 独立网站

## 冻结结论

当前版本冻结为**整改与复验中的待验收测试版**，只用于内部验收和演示，不作为正式商城发布。原版本已被独立核验判定未通过完整销售闭环；本轮整改不等于正式上线批准。

## 冻结范围

- 暂不正式上线；
- 不修改 `xiaominART.com` / `xiaominart.com` 正式域名解析；
- 不接入真实收款、银行卡、生产支付回调或真实订单履约；
- 不导入真实客户资料；
- 不将演示商品、演示价格、演示库存和演示图片当作正式商业资料；
- 不修改 Geektion 项目；xiaominART 与 Geektion 完全独立；
- 后续如需改动冻结版本，先记录变更原因并重新验收，不直接进入正式上线。

## 当前访问方式

### 可复现启动地址

在 `xiaominart-test-site/` 目录运行：

```bash
npm run dev
```

默认地址：`http://localhost:4321/`

### 当前临时运行地址

当前隔离运行地址：`http://127.0.0.1:4334/`

后台：`http://127.0.0.1:4334/admin/login`

这是本机临时地址，服务停止后失效；不是公网测试站，也不是正式域名。

## 支付声明

测试结算使用**内部模拟支付**，提供模拟成功、模拟失败和模拟取消三种状态。没有接入第三方支付沙盒，也没有验证真实支付网关；“模拟支付成功”只表示测试订单状态成功，不代表真实付款、真实收入或支付集成已验证。

## 解冻条件

只有在用户明确批准后，才可进入下一阶段。至少需要：真实商品与素材确认、经营配置确认、正式政策审阅、生产安全与备份验证、真实支付沙盒批准，以及明确的域名切换批准。
